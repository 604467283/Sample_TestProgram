#####COM

            if (!COM1.RunCommandLine("AT+EGMR=0,7\r\n", data =>
            {
                imei = data.Replace("AT+EGMR", "").Replace("+", "").Replace("EGMR", "").Replace("OK", "").Replace("\"", "").Replace("\r", "").Replace("\n", "").Replace(":", "").Replace("+", "").Trim();
                return StringTool.IsImeiNumber(imei);
            }, out string message, 3000, 5))
            {
                record.Cause = "get imei failed!";
                return false;
            }

###ssh

              try
              {

                  SshClient = new SshHelper("shipeng.familyds.com", 6783,"home", "home");
                  SshClient.Connect();

                  if (!SshClient.IsConnected)
                  {
  
                          return false;
         
                  }
                  SshClient.RunCommandLine("ls", "Photo", out string msg, 3000, 3);

              }
              catch (Exception ex)
              {
                  MessageBox.Show(ex.Message);
                  return false;
              }


  ####sfc log 

            DataHandler dataHandler = new DataHandler();    
            Collection_LogData.ACE_Log.DataExtraction dataExtraction = new();

            bool bResultLog = false;

            dataHandler = dataExtraction.Data_Extraction(@"C:\Users\Administrator\Desktop\New\Liteon_TestProgram\Liteon_TestProgram\bin\Debug\net6.0-windows\18CC8882E7F6A_20240930_123346.txt", ref bResultLog);

            dataHandler.ProcessDataToSFCFile(@"E:\1.txt", true, "1122", "AAAA", "V1.0", "");


######FTP

           var ftpHelper = new FTPHelper("ftp.example.com", 21, "username", "password");

            if (ftpHelper.Login())
            {
                Console.WriteLine("FTP Login Successful");

                bool uploadSuccess = ftpHelper.UploadFile(@"C:\local\path\to\yourfile.txt", "/remote/path/to/yourfile.txt");
                if (uploadSuccess)
                    Console.WriteLine("File Uploaded Successfully");

                bool downloadSuccess = ftpHelper.DownloadFile("/remote/path/to/anotherfile.txt", @"C:\local\path\to\downloadedfile.txt");
                if (downloadSuccess)
                    Console.WriteLine("File Downloaded Successfully");
            }
            else
            {
                Console.WriteLine("FTP Login Failed");
            }


########HttpWithBodyHelper

            HttpWithBodyHelper httpClientHelper = new();  

            string url = "https://www.baidu.com/s";

            var body = new  //这里构建body
            {
                wd = "1"
            };

            try
            {
                string responseJson = await HttpClientHelper.PostJsonAsync(url, body);

                Console.WriteLine($"Message: {responseJson}");

            }
            catch (HttpRequestException e)
            {
                Console.WriteLine($"Request error: {e.Message}");
            }


########## HttpMesHelper
         static async Task Main(string[] args)
         {
            HttpClientHelper httpClientHelper = new HttpClientHelper();
            var response = await httpClientHelper.PostWithTimeoutAsync(HttpClientHelper.MesPostType.ADD, "123456789A", "P", "Test2");
            Console.WriteLine(response);

            var response1 = await httpClientHelper.PostWithTimeoutAsync(HttpClientHelper.MesPostType.QUERY, "123456789A", "P");
            Console.WriteLine(response1);

            Console.ReadLine();
         }


########## CustomProcess

            CustomProcess customProcess = new($"{str_ReadKBCVerExeFolderPath}\\{str_ReadKBCVerExeName}");
            bRetVal = customProcess.RunCommandLine("", "(TestOnly)", out string msg, 5000, 3);

            if (bRetVal)
            {
                if (msg.Contains(";PASS"))
                {
                    UIHandleHelper.ShowRunLog($"KBC ver check pass: {msg}",
                                                             FailColor: false,
                                                             ShowGridView: true,
                                                             TestItemName: "Check Ver",
                                                             TestItemContent: $"KBC ver check pass: {msg}",
                                                             TestItemResult: true);
                }
                else
                {
                    UIHandleHelper.ShowRunLog($"KBC ver check fail: {msg}", 
                                                               FailColor: true,
                                                               ShowGridView: true,
                                                               TestItemName: "Check Ver",
                                                               TestItemContent: $"KBC ver check fail: {msg}",
                                                               TestItemResult: false,
                                                               ErrorCode:TestErrorCode.ErrorCode.Err030.ToString());

                    return false;
                }
            }
            else
            {
                UIHandleHelper.ShowRunLog($"KBC ver check error: {msg}",
                                                              FailColor: true,
                                                              ShowGridView: true,
                                                              TestItemName: "Check Ver",
                                                              TestItemContent: $"KBC ver check error: {msg}",
                                                              TestItemResult: false,
                                                              ErrorCode: TestErrorCode.ErrorCode.Err030.ToString());

                return false;
            }


######SQLite Sample

             string databasePath = "example.db";
            SQLiteHelper sqliteHelper = new SQLiteHelper(databasePath);

            // 创建表
            string createTableQuery = "CREATE TABLE IF NOT EXISTS Students (Id INTEGER PRIMARY KEY, Name TEXT)";
            sqliteHelper.CreateTable("Students", createTableQuery);

            // 增加列
            string addColumnQuery = "Age INTEGER";
            sqliteHelper.AddColumn("Students", "Age", addColumnQuery);

            // 插入数据
            string insertDataQuery = "INSERT INTO Students (Name, Age) VALUES ('John Doe', 20)";
            sqliteHelper.InsertData("Students", insertDataQuery);

            // 更新数据
            string updateDataQuery = "UPDATE Students SET Age = 21 WHERE Name = 'John Doe'";
            sqliteHelper.UpdateData("Students", updateDataQuery);

            // 查询数据
            string selectQuery = "SELECT * FROM Students";
            sqliteHelper.QueryData("Students", selectQuery);



########ThirdPartyWindow_Helper

            IntPtr hwnd = IntPtr.Zero;
            //实例
            ThirdPartyWindowEnumeration_Helper thirdPartyWindow_Helper = new();

            //找标题为Form1的窗口，也可以按className找
            hwnd =thirdPartyWindow_Helper.FindExpectWindow("Form1");

            //使用找到的窗口的句柄，按枚举的个数来确定需要控件，再修改它的text，一般用于textbox等
            thirdPartyWindow_Helper.FindAndSetControlText(hwnd, 15, "Sample");

            #####Or######

            //使用找到的窗口的句柄，按控件名称枚举来确定需要控件，再修改它的text，一般用于textbox等
            thirdPartyWindow_Helper.FindAndSetControlText(hwnd, "TextBox1", "Sample");

            ////使用找到的窗口的句柄，按枚举的个数来确定需要控件, 再获取控件Text，没有找到返回null
            string str = thirdPartyWindow_Helper.GetControlText(hwnd, 15);

            //使用找到的窗口的句柄，按枚举的个数来确定需要控件，再发送回车给它，一般用于按钮等；
            thirdPartyWindow_Helper.SendControlEnter(hwnd, 18);

            #####Or######
            
            //使用找到的窗口的句柄，按控件名称枚举来确定需要控件，再发送回车给它，一般用于按钮等；
            thirdPartyWindow_Helper.SendControlEnter(hwnd, "button1");



###############PEMController

            PEMController pEMController = new PEMController();
            pEMController.UILogMessage = UIHandleHelper.ShowRunLog; // 绑定日志输出方法

            if(pEMController.Initialize())
            {
                 pEMController.PowerOn();
                  pEMController.PowerOff();
                  pEMController.Exit();
            }

            ..... ....

         protected override void Dispose(bool disposing)
         {
            if (disposing)
            {
                pEMController?.Dispose(); // 释放 pEMController 资源
            }
            base.Dispose(disposing);
         }


#################### SFCHelpers 检查站别

              //获取help的提示
             CustomProcess customProcess = new(".\\SFCHelpers.exe");
             bool bRetVal = customProcess.RunCommandLine("-help", "===END===", out string msg, 5000, 3); //获取全部返回值

             if(msg.Contain("期望的值"))
             {
                return true;
             }

             //获取检查站别的信息
             bool bRetVal = customProcess.RunCommandLine("-init -station 60007831 -ip 10.141.106.194 -check -mac 443E075B0216 -mo 97603-002070A000 -destroy", "===END===", out string msg, 5000, 3);
             
             if(msg.Contain("期望的值"))
             {
                return true;
             }


####################RockMongGpioController  只支持3.3V的IO

             // 创建控制器(自动选择第一个设备). 
                using var gpio = new SimpleGpioController();

                // 1. 读取单个引脚
                bool pin5State = gpio.ReadPin(5);
                Console.WriteLine($"Pin 5 state: {pin5State}");

                // 2. 读取多个指定引脚
                var pinsToRead = new[] { 0, 2, 4, 6, 8 };
                 bool[] multiplePinStates = gpio.ReadMultiplePins(pinsToRead);

                for (int i = 0; i < multiplePinStates.Length; i++)
                {
                    Console.WriteLine($"Pin {i}: {allPinStates[i]}");
                }

                // 3. 读取所有引脚
                bool[] allPinStates = gpio.ReadAllPins();
                for (int i = 0; i < allPinStates.Length; i++)
                {
                    Console.WriteLine($"Pin {i}: {allPinStates[i]}");
                }


####################  TCPIP_ClientHelper
              
              static async Task Main(string[] args)
             {
                    try
                    {
                        // 创建TCP客户端实例
                        using var tcpClient = new TcpClientWrapper("127.0.0.1", 8080);
            
                        // 连接到服务器
                        await tcpClient.ConnectAsync();
                        Console.WriteLine("已连接到服务器");
            
                        // 发送数据并接收响应
                        string response = await tcpClient.SendAndReceiveAsync("Hello, Server!");
                        Console.WriteLine($"收到响应: {response}");
            
                        // 单独发送数据
                        await tcpClient.SendAsync("Another message");
            
                        // 单独接收数据
                        string anotherResponse = await tcpClient.ReceiveAsync();
                        Console.WriteLine($"收到另一个响应: {anotherResponse}");
                    }
                    catch (Exception ex)
                    {
                        Console.WriteLine($"发生错误: {ex.Message}");
                    }
            }



  ####################  DevconHelper

            // 示例1：获取设备ID并操作
            string wifiAdapterId = DevconHelper.GetDeviceIdByName("Intel Wi-Fi 6 AX201");
            if (wifiAdapterId != null)
            {
                DevconHelper.Disable(wifiAdapterId);  // 禁用
                System.Threading.Thread.Sleep(2000);
                DevconHelper.Enable(wifiAdapterId);   // 重新启用
            }

            // 示例2：直接按名称禁用USB设备
            DevconHelper.DisableByName("Realtek USB 3.0 Card Reader");

            // 示例3：刷新所有设备
            DevconHelper.Rescan();


#####################NetworkAdapterHelper

			1. 获取所有网卡信息
            var allAdapters = NetworkAdapterHelper.GetAllAdapters();
            foreach (var adapter in allAdapters)
            {
                Console.WriteLine($"名称: {adapter.Name}");
                Console.WriteLine($"描述: {adapter.Description}");
                Console.WriteLine($"类型: {adapter.Type}");
                Console.WriteLine($"MAC: {adapter.MacAddress}");
                Console.WriteLine("------------------------");
            }

            2. 获取所有 Wi-Fi 网卡
            var wifiAdapters = NetworkAdapterHelper.GetWifiAdapters();
            foreach (var wifi in wifiAdapters)
            {
                Console.WriteLine($"Wi-Fi 描述: {wifi.Description}, MAC: {wifi.MacAddress}");
            }

            3. 根据描述名称获取 MAC 地址
            // 查找描述包含 "Intel" 的网卡 MAC 地址
            string mac = NetworkAdapterHelper.GetMacAddressByDescription("Intel(R) Wi-Fi 6 AX201");
            Console.WriteLine($"MAC 地址: {mac}");

            // 严格匹配大小写
            string mac2 = NetworkAdapterHelper.GetMacAddressByDescription("intel(r) wi-fi 6 ax201", ignoreCase: false);
            Console.WriteLine($"MAC 地址（严格匹配）: {mac2}");